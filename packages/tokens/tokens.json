{
  "$description": "Design tokens, in the W3C Design Tokens Format Module shape (v2025.10). THE authority for every design value in the product. packages/tokens/generated/tokens.css is DERIVED from this file and is never hand-edited (law 27). Three layers, and the middle one is the point: primitives are raw values, semantics name a ROLE, and components reference semantics. A screen that reaches past the semantic layer to a primitive has hardcoded a decision the design system is supposed to own -- the generator refuses that edge rather than trusting it not to happen.",

  "color": {
    "$type": "color",
    "$description": "PRIMITIVE layer. Raw material with no opinion about use, which is why there is no `color.error` here: a primitive named for a meaning has taken the semantic layer's job, and the day dark mode arrives it is the wrong colour under a name nobody can change.",
    "neutral": {
      "0": { "$value": "#ffffff" },
      "50": { "$value": "#f8fafc" },
      "100": { "$value": "#f1f5f9" },
      "200": { "$value": "#e2e8f0" },
      "400": { "$value": "#94a3b8" },
      "600": { "$value": "#475569" },
      "800": { "$value": "#1e293b" },
      "900": { "$value": "#0f172a" },
      "950": { "$value": "#020617" }
    },
    "blue": {
      "100": { "$value": "#dbeafe" },
      "600": { "$value": "#2563eb" },
      "700": { "$value": "#1d4ed8" },
      "950": { "$value": "#172554" }
    },
    "red": {
      "50": { "$value": "#fef2f2" },
      "200": { "$value": "#fecaca" },
      "400": { "$value": "#f87171" },
      "700": { "$value": "#b91c1c" },
      "950": { "$value": "#450a0a" }
    },
    "amber": {
      "50": { "$value": "#fffbeb" },
      "200": { "$value": "#fde68a" },
      "300": { "$value": "#fcd34d" },
      "800": { "$value": "#92400e" },
      "950": { "$value": "#451a03" }
    }
  },

  "space": {
    "$type": "dimension",
    "$description": "PRIMITIVE layer. A 4px scale, so spacing is chosen from a set rather than invented per screen.",
    "0": { "$value": "0" },
    "1": { "$value": "0.25rem" },
    "2": { "$value": "0.5rem" },
    "3": { "$value": "0.75rem" },
    "4": { "$value": "1rem" },
    "5": { "$value": "1.5rem" },
    "6": { "$value": "2rem" }
  },

  "size": {
    "$type": "dimension",
    "$description": "PRIMITIVE layer.",
    "radius-sm": { "$value": "0.25rem" },
    "radius-md": { "$value": "0.5rem" },
    "border": { "$value": "1px" },
    "focus-ring": { "$value": "2px" },
    "focus-offset": { "$value": "2px" },
    "text-sm": { "$value": "0.875rem" },
    "text-md": { "$value": "1rem" },
    "text-lg": { "$value": "1.25rem" },
    "measure": { "$value": "42rem" },
    "target-min": {
      "$value": "1.5rem",
      "$description": "24px at a 16px root. WCAG 2.5.8's minimum interactive target. A primitive so that the semantic floor cannot be set to something smaller by accident -- and a generator invariant asserts the floor holds in EVERY density mode, because compact is exactly where somebody would shave it."
    }
  },

  "font": {
    "$type": "fontFamily",
    "$description": "PRIMITIVE layer. A system stack: no web font on the critical path. The employee route has 33.6 kB of headroom under the 180 kB budget, and a font is the easiest way to spend a chunk of it for nothing.",
    "sans": {
      "$value": "ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif"
    },
    "mono": {
      "$value": "ui-monospace, SFMono-Regular, Menlo, Consolas, monospace"
    }
  },

  "semantic": {
    "$description": "SEMANTIC layer. Names a ROLE, not a value. This is the layer the stylesheet and the component tier are allowed to use, and the reason a theme change is a token change rather than a sweep through every screen. Two independent axes rebind this layer: THEME rebinds colour, DENSITY rebinds geometry. Nothing rebinds both.",

    "surface": {
      "$type": "color",
      "page": { "$value": "{color.neutral.50}" },
      "raised": { "$value": "{color.neutral.0}" },
      "sunken": { "$value": "{color.neutral.100}" }
    },
    "text": {
      "$type": "color",
      "default": { "$value": "{color.neutral.900}" },
      "muted": { "$value": "{color.neutral.600}" },
      "on-accent": { "$value": "{color.neutral.0}" }
    },
    "border": {
      "$type": "color",
      "$description": "COLOUR only. The width of a border is geometry and lives at semantic.stroke.width, because the two change on different axes and a group that mixed them would be overridden by both.",
      "default": { "$value": "{color.neutral.200}" },
      "strong": { "$value": "{color.neutral.400}" }
    },
    "accent": {
      "$type": "color",
      "default": { "$value": "{color.blue.600}" },
      "hover": { "$value": "{color.blue.700}" },
      "subtle": { "$value": "{color.blue.100}" }
    },
    "danger": {
      "$type": "color",
      "$description": "For a state that has gone wrong. NOT for a destructive action button, which needs its own role when one exists.",
      "text": { "$value": "{color.red.700}" },
      "surface": { "$value": "{color.red.50}" },
      "border": { "$value": "{color.red.200}" }
    },
    "warning": {
      "$type": "color",
      "$description": "For a state the user must reconcile -- a version conflict, a stale projection. Distinct from danger: nothing is broken, but something needs a decision.",
      "text": { "$value": "{color.amber.800}" },
      "surface": { "$value": "{color.amber.50}" },
      "border": { "$value": "{color.amber.200}" }
    },
    "focus": {
      "$type": "color",
      "$description": "The focus ring is a token, so it cannot be styled away per screen. Keyboard operability is a property of the system, not a per-component courtesy.",
      "ring": { "$value": "{color.blue.600}" }
    },

    "space": {
      "$type": "dimension",
      "$description": "GEOMETRY. The density axis rebinds these, which is why the stylesheet must never reach the space primitives directly -- there would be nothing in between for compact to change.",
      "stack-tight": { "$value": "{space.2}" },
      "stack": { "$value": "{space.4}" },
      "stack-loose": { "$value": "{space.5}" },
      "inline": { "$value": "{space.4}" },
      "inline-tight": { "$value": "{space.1}" },
      "section": { "$value": "{space.5}" }
    },
    "control": {
      "$type": "dimension",
      "$description": "GEOMETRY of an interactive control -- button, input, combobox trigger.",
      "padding-block": { "$value": "{space.2}" },
      "padding-inline": { "$value": "{space.3}" }
    },
    "row": {
      "$type": "dimension",
      "$description": "GEOMETRY of a repeated row: a list item today, a grid row in stage 6.",
      "padding": { "$value": "{space.3}" },
      "gap": { "$value": "{space.4}" }
    },
    "container": {
      "$type": "dimension",
      "$description": "GEOMETRY of a bounded region. Separate from `surface`, which is that region's COLOUR and moves on the theme axis instead.",
      "padding": { "$value": "{space.5}" },
      "measure": { "$value": "{size.measure}" }
    },
    "type": {
      "$type": "dimension",
      "$description": "GEOMETRY of text. Density may tighten these; theme never touches them.",
      "label": { "$value": "{size.text-sm}" },
      "body": { "$value": "{size.text-md}" },
      "heading": { "$value": "{size.text-lg}" }
    },
    "radius": {
      "$type": "dimension",
      "control": { "$value": "{size.radius-sm}" },
      "container": { "$value": "{size.radius-md}" }
    },
    "stroke": {
      "$type": "dimension",
      "$description": "Border WIDTH. See the note on semantic.border, which owns border colour.",
      "width": { "$value": "{size.border}" },
      "focus-ring": { "$value": "{size.focus-ring}" },
      "focus-offset": { "$value": "{size.focus-offset}" }
    },
    "font": {
      "$type": "fontFamily",
      "$description": "Typeface ROLES. Owned by neither axis -- a theme does not change the typeface and neither does density -- but they belong here so the stylesheet has no reason to reach a primitive.",
      "body": { "$value": "{font.sans}" },
      "code": { "$value": "{font.mono}" }
    },
    "target": {
      "$type": "dimension",
      "$description": "The minimum interactive target. WCAG 2.5.8 permits 24x24 CSS px or a documented exception; Xforge takes the stricter house rule that system targets meet it in EVERY density mode. Tokens can only guarantee the values a component is built from -- a 20px icon button can still be composed from compliant tokens -- so the rendered check belongs to a later stage and this is the static half.",
      "minimum": { "$value": "{size.target-min}" }
    }
  },

  "component": {
    "$description": "COMPONENT layer. A token belongs here only when changing THAT component independently of its semantic category is a supported operation. It is deliberately small and deliberately GEOMETRY-ONLY: colour stays semantic, so dark mode rebinds a handful of roles and every component follows, instead of the component tier being duplicated per theme. A count ceiling makes growth something a human looks at.",

    "button": {
      "$type": "dimension",
      "padding-block": { "$value": "{semantic.control.padding-block}" },
      "padding-inline": { "$value": "{semantic.control.padding-inline}" },
      "min-size": { "$value": "{semantic.target.minimum}" }
    },
    "card": {
      "$type": "dimension",
      "padding": { "$value": "{semantic.container.padding}" }
    },
    "alert": {
      "$type": "dimension",
      "padding-block": { "$value": "{semantic.control.padding-block}" },
      "padding-inline": { "$value": "{semantic.space.inline}" }
    },
    "list-item": {
      "$type": "dimension",
      "padding": { "$value": "{semantic.row.padding}" },
      "gap": { "$value": "{semantic.row.gap}" }
    }
  }
}
