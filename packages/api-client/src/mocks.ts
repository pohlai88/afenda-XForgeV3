/**
 * MSW handlers, generated from the same contract as the client.
 *
 * This file is why the frontend can be finished before a handler or a database
 * exists -- the single highest-leverage decision in the architecture, and the
 * reason a client generator without mocks was rejected.
 */
export * from './generated/xforge.msw'
